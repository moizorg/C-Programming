#include <stdio.h>
int main()
{
    char state;
    do{
        printf("\nPlease enter the current state of the telephone line (enter \'w\' for working and \'d\' for dead) >");
    }
    while(state != 'w' && atate != 'd');
    return 0;
}
